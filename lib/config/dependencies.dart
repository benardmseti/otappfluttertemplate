import 'package:provider/single_child_widget.dart';

/// Configure providers/dependencies for app functionalities
///
List<SingleChildWidget> get providers {
  return [
    // e.g.
    // Provider(create: (context) => SomeProvider());
  ];
}
